# Copy to .env and fill in the real values.

# From @BotFather
TELEGRAM_BOT_TOKEN=

# Numeric chat id — the user or channel that should receive notifications
TELEGRAM_CHAT_ID=

# Comma-separated origins allowed to call /api/contact
ALLOWED_ORIGINS=https://valeriy-dev.xyz

# Per-IP submissions allowed per hour
RATE_LIMIT_PER_HOUR=5
